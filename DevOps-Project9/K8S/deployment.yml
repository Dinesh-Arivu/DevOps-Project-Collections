apiVersion: apps/v1
kind: Deployment
metadata:
  name: amazon-deployment
spec:
  replicas: 3  # Number of desired replicas
  selector:
    matchLabels:
      app: amazon
  template:
    metadata:
      labels:
        app: amazon
    spec:
      containers:
      - name: amazon
        image: dinesh1097/amazon:latest
        ports:
        - containerPort: 3000  # Port your application listens on
